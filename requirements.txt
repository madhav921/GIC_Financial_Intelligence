# GIC Financial Intelligence — Vercel deployment dependencies
# Slim subset: ~300 MB installed, fits within Vercel's 500 MB free limit.
# Excludes: polars, pyarrow, ccxt, yfinance, fredapi, ffn, scipy, statsmodels
#   - scipy replaced by numpy grid search in hedge_optimizer.py
#   - statsmodels (SARIMAX) is a lazy import; falls back gracefully to XGBoost
# For local dev / Docker use requirements-local.txt (full ML stack).

# ── Data Processing ──
numpy>=1.24.0
pandas>=2.0.0

# ── ML ──
scikit-learn>=1.3.0
xgboost>=2.0.0

# ── API & Web ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.25.0
python-multipart>=0.0.9

# ── Infrastructure ──
pyyaml>=6.0
loguru>=0.7.0
joblib>=1.3.0
python-dotenv>=1.0.0
apscheduler>=3.10.0
