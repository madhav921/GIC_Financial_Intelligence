{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "comment": "Backend-only Vercel deployment. The React frontend is deployed separately from frontend/ using frontend/vercel.json.",
  "builds": [
    {
      "src": "src/api/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/api/app.py"
    }
  ]
}
